apiVersion: 1

datasources:
  - name: Prometheus Baseline
    uid: prom-baseline
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false
    jsonData:
      httpMethod: POST
      prometheusType: Prometheus
      timeInterval: 10s
